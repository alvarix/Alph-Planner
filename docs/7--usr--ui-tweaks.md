- Backlog: drag and drop order; fold

    - I want to be able to reorder tasks in the backlog, as well as order of categories.
    - backlog should be treated as same type of object as day - both have the same functionality
    - should be able to change order of categories
- category folding
- new view option:
    - switch to rest of days only. ie hide past days. Animate transition. 
    assess if this is longer than 2h, not worth it
